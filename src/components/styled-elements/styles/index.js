import {
    defaultBackground,
    elementBackground,
    overlayBackground,
} from './Backgrounds';
import {
    borderColor,
    borderRadius,
    borderStyle,
    regularBorders,
} from './Borders';
import {
    equalShadow,
    hoverShadow,
    shadow,
} from './Shadows';
import {
    textColor,
} from './Text';

export {
    borderColor,
    borderRadius,
    borderStyle,
    defaultBackground,
    elementBackground,
    equalShadow,
    hoverShadow,
    overlayBackground,
    regularBorders,
    shadow,
    textColor,
};
