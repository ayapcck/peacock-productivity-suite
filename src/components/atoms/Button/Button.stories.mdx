import {
    ArgsTable,
    Canvas,
    Meta,
    Story,
} from '@storybook/addon-docs/blocks';

import Button from './Button';
import * as stories from './Button.stories.js';

<Meta
    component={Button}
    title="Atoms/Button"
/>

export const Primary = (args) => <Button { ...args } />;

# Button

This is a basic button
<Canvas>
    <Story
        name="Primary"
        args={{
            text: 'Test Button'
        }}
    >
        { Primary.bind({}) }
    </Story>
</Canvas>

<ArgsTable story="Primary" />

These are the button variants with Dark and Light themes respectively:
<Story story={stories.Demo} />
