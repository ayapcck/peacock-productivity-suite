import { actions } from './slices';

const {
    closeAuthentication,
    closeLoadingOverlay,
    loadAuthentication,
    loginUser,
    logoutUser,
    openAuthentication,
    openLoadingOverlay,
    updateAuthentication,
} = actions;

export {
    closeAuthentication,
    closeLoadingOverlay,
    loadAuthentication,
    loginUser,
    logoutUser,
    openAuthentication,
    openLoadingOverlay,
    updateAuthentication,
};
