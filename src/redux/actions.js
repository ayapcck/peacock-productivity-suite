import { actions } from './slices';

const {
    closeAuthentication,
    closeLoadingOverlay,
    loginUser,
    logoutUser,
    openAuthentication,
    openLoadingOverlay,
} = actions;

export {
    closeAuthentication,
    closeLoadingOverlay,
    loginUser,
    logoutUser,
    openAuthentication,
    openLoadingOverlay,
};
