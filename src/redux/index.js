import {
    closeAuthentication,
    loginUser,
    logoutUser,
    openAuthentication,
} from './actions';
import store from './store';

export {
    closeAuthentication,
    loginUser,
    logoutUser,
    openAuthentication,
};

export default store;
